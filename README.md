# Face Mask Detection (CNN)

This project implements a Convolutional Neural Network (CNN) to detect
whether a person is wearing a face mask or not.

## Dataset
- with_mask
- without_mask

## Model
- CNN (TensorFlow / Keras)
- Data Augmentation
- Early Stopping

## Results
- Validation accuracy ~91%
- Test accuracy ~90%

